package Model;

public class HashtablePosition {
    int hashPosition;
    int slot;

    public HashtablePosition(int hashPosition, int slot) {
        this.hashPosition = hashPosition;
        this.slot = slot;
    }
}
